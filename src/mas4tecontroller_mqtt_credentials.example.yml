# Copy to mas4tecontroller_mqtt_credentials.yml and fill in the credentials.
#
# Only read when MQTT_ONLINE is true, i.e. when the battery client talks to the
# live Jülich broker over TLS instead of a local broker. Left alone in the
# default offline setup.
username: replace-me
password: replace-me
